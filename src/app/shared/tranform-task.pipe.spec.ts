import { TranformTaskPipe } from './tranform-task.pipe';

describe('TranformTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new TranformTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
